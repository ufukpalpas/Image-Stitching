UFUK PALPAS 21702958
To run the code create 2 text files named as in Code and these txt files shouldnt be empty
The images need to be sorted (reverse in fishbowl)
The images need to be seperate in 2 different folder as the named in given hw2 file 
the os reads images from there
To run the code there is no options and images need to be sorted according to left in current version

The project uses OpenCV and numpy to install use:
pip install opencv
pip install numpy